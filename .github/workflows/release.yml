name: Release

on:
  # Fires on any git tag push — we use CalVer (e.g. 2026.4.21), but this
  # also covers plain v* tags or anything else someone pushes. The
  # setuptools-scm integration in pyproject.toml reads whatever tag you
  # pushed and bakes it into the built wheel.
  push:
    tags: ["*"]
  workflow_dispatch:

jobs:
  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          # setuptools-scm needs the full git history to resolve the
          # current tag into a version string.
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build sdist
        # Forwarding the tag name as SETUPTOOLS_SCM_PRETEND_VERSION means
        # `python -m build` doesn't have to shell out to git to discover
        # the version. Leaving it unset lets setuptools-scm fall back to
        # the dev-stamped version on workflow_dispatch runs.
        env:
          SETUPTOOLS_SCM_PRETEND_VERSION: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
        run: |
          python -m pip install --upgrade pip build
          python -m build --sdist

      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*.tar.gz

  build_wheels:
    name: Build wheels on ${{ matrix.os }} (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux amd64 + arm64.
          - os: ubuntu-latest
            arch: x86_64
          - os: ubuntu-24.04-arm
            arch: aarch64
          # macOS arm64 only. Intel Mac (x86_64) is skipped — modern
          # Macs are all Apple Silicon. The deployment target is 14.0
          # because that's the minimum macOS the Homebrew openssl@3
          # bottle ships with; delocate refuses to downgrade it.
          - os: macos-14
            arch: arm64
          # Windows support is deferred — the bundled libdatachannel
          # dynamically links OpenSSL's DLLs and we haven't wired up the
          # delvewheel / os.add_dll_directory dance needed to make that
          # work on end-user machines.
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          # setuptools-scm needs the full git history to resolve the
          # current tag into a version string.
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # cibuildwheel v3 added the runtime images for CPython 3.14 + the
      # AlmaLinux 9 (manylinux_2_34) base used below. v2.21 ships
      # neither.
      - uses: pypa/cibuildwheel@v3.4
        env:
          # cibuildwheel tars the project and runs pip inside a manylinux
          # container — git history isn't available there, so we forward
          # the tag name directly and ask setuptools-scm to use it.
          SETUPTOOLS_SCM_PRETEND_VERSION: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
          CIBW_ENVIRONMENT_PASS_LINUX: "SETUPTOOLS_SCM_PRETEND_VERSION"
          CIBW_ARCHS: ${{ matrix.arch }}
          # Nanobind's stable-ABI mode emits one cp312-abi3-<platform>
          # wheel per (os, arch) that covers every CPython 3.12+. Listing
          # cp312 / cp313 / cp314 explicitly doesn't multiply the wheels —
          # cibuildwheel deduplicates by abi3 tag — but it does run the
          # test command under each interpreter, which is the only thing
          # that would have caught the OpenSSL 1.1 / 3.x cohabit crash
          # that surfaced only on CPython 3.14.
          CIBW_BUILD: "cp312-* cp313-* cp314-*"
          CIBW_SKIP: "*-musllinux_*"
          # AlmaLinux 9 (manylinux_2_34) ships OpenSSL 3.x. The previous
          # manylinux_2_28 base baked in OpenSSL 1.1.1, which auditwheel
          # bundled into the wheel; on hosts that also load the system
          # OpenSSL 3 (Python's ``_ssl`` does, transitively), the
          # libdatachannel static link would crash on the first
          # ``PeerConnection`` constructor under CPython 3.14. Match the
          # bundled OpenSSL to what modern hosts already run.
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux_2_34
          CIBW_MANYLINUX_AARCH64_IMAGE: manylinux_2_34
          CIBW_BEFORE_ALL_LINUX: "yum install -y openssl-devel"
          CIBW_BEFORE_ALL_MACOS: "brew install openssl@3"
          CIBW_ENVIRONMENT_MACOS: >-
            OPENSSL_ROOT_DIR=$(brew --prefix openssl@3)
            MACOSX_DEPLOYMENT_TARGET=14.0
          CIBW_TEST_REQUIRES: "pytest pytest-asyncio"
          # Smoke suite only — stress tests are skipped at this layer
          # because they reliably hang inside cibuildwheel's
          # manylinux_2_28 docker container (the same tests complete
          # in ~70 s natively on the same host runner). Suspected
          # container-specific SCTP/thread interaction we don't want
          # to debug under release-time pressure. Stress coverage
          # still runs via the CI workflow's ``tests-stress`` job
          # (nightly cron + on-demand via label/workflow_dispatch) on
          # the host runner without docker, so regressions are caught
          # before the next release cut — just not on the release tag
          # itself.
          CIBW_TEST_COMMAND: >-
            AIOLIB_REQUIRE_NATIVE=1
            pytest {project}/tests -v -m "not stress"
          # ``cp314-*`` is excluded because the cibuildwheel cp314
          # interpreter (a from-source CPython 3.14 baked into the
          # manylinux_2_34 image) segfaults inside libdatachannel's ICE
          # gathering on the very first ``test_ice_candidates_*`` test —
          # while the same wheel passes the same test under stock
          # CPython 3.14 (see the ``smoke_wheels`` job below). Until the
          # upstream cibuildwheel/manylinux 3.14 image ships a stable
          # interpreter, we keep the wheel-build green by skipping its
          # in-container 3.14 tests and lean on ``smoke_wheels`` to
          # cover that target.
          # ``*-macosx_*`` skip survives because loopback UDP perms
          # aren't reliable on hosted macOS runners.
          CIBW_TEST_SKIP: "*-macosx_* cp314-*"

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}-${{ matrix.arch }}
          path: wheelhouse/*.whl

  smoke_wheels:
    # Re-test the just-built Linux wheels under stock CPython interpreters
    # (actions/setup-python). Catches wheel-level regressions on Python
    # versions whose cibuildwheel test environment we had to skip — at
    # the moment that's CPython 3.14, where the in-container cp314
    # interpreter from manylinux_2_34 segfaults inside libdatachannel
    # but the same wheel passes the same suite on a stock 3.14.
    name: Smoke-test wheel (Python ${{ matrix.python }}, ${{ matrix.arch }})
    needs: [build_wheels]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64
            python: "3.14"
          - os: ubuntu-24.04-arm
            arch: aarch64
            python: "3.14"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - uses: actions/download-artifact@v4
        with:
          name: wheels-${{ matrix.os }}-${{ matrix.arch }}
          path: wheelhouse
      - name: Install wheel + smoke test
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-asyncio
          # The wheel filename is abi3-tagged so a single wheel covers
          # cp312+; install the manylinux artefact straight from the
          # wheelhouse dir.
          pip install wheelhouse/aiolibdatachannel-*-cp312-abi3-manylinux*.whl
          AIOLIB_REQUIRE_NATIVE=1 pytest tests -v -m "not stress"

  publish:
    name: Publish to PyPI
    needs: [build_sdist, build_wheels, smoke_wheels]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    environment:
      name: pypi
      url: https://pypi.org/p/aiolibdatachannel
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
